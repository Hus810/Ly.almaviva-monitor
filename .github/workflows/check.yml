name: Almaviva Monitor

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install puppeteer node-fetch@2
      - run: node bot.js
        env:
          BOT_TOKEN: ${{ 8590385391:AAHHlhq095SxtNbYFfMEx2x2LlZU1yKSmTk }}
          CHAT_ID: ${{ 1602191275 }}
          EMAIL: ${{ h.alhamdi88@gmail.com }}
          PASSWORD: ${{ Aa@8102290 }}
